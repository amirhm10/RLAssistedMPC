# 2026-04-21 Widen Polymer Matrix And Structured Defaults

- widened polymer plain matrix multiplier defaults from `0.95-1.05` to `0.85-1.20`
- widened the shared structured `wide` range profile so all structured `A` and `B` multipliers use `0.85-1.20`
- switched polymer structured-matrix defaults to `range_profile="wide"`
- switched distillation structured-matrix defaults to `range_profile="wide"` so the structured family stays aligned across systems
