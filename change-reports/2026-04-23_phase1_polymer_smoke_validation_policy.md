# 2026-04-23 Phase 1 Polymer Smoke Validation Policy

- Added an `AGENTS.md` rule to avoid opening the distillation Aspen column for routine smoke tests.
- For shared unified runner changes, polymer smoke validation is now the default runtime confirmation path unless a full distillation execution is explicitly requested.
- Confirmed the Phase 1 TD3 hidden-release behavior on polymer `matrix`, `structured_matrix`, and `residual` smoke executions using isolated temporary output roots.
